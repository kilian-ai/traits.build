// @generated from skills.trait.toml — DO NOT EDIT
// Regenerate with: cargo run -p gen-wit
//
// Trait path: llm.agent.skills
// Source version: v260402.115951

package traits:llm-skills@0.1.0;

/// Return raw content from all SKILL.md files — for LLM agent context
interface skills {
  /// Return raw content from all SKILL.md files — for LLM agent context
  ///
  /// Parameters:
  /// - `name`: string (optional) — Skill name (e.g. 'traits.build', 'secrets'). Use 'list' for index, empty/all for everything.
  ///
  /// Returns: { ok, skills: [{name, description, content}] } or { ok, name, description, content } for single skill
  skills: func(
    name: option<string>
  ) -> result<string, string>;
}

world skills-world {
  export skills;
}
