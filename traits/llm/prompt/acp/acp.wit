// @generated from acp.trait.toml — DO NOT EDIT
// Regenerate with: cargo run -p gen-wit
//
// Trait path: llm.prompt.acp
// Source version: v260329.170906

package traits:llm-acp@0.1.0;

/// Route prompts to ACP agents via chrome-acp proxy (OpenCode, Claude Code, Codex, Copilot)
interface acp {
  /// Route prompts to ACP agents via chrome-acp proxy (OpenCode, Claude Code, Codex, Copilot)
  ///
  /// Parameters:
  /// - `prompt`: string — User message to send to the ACP agent
  /// - `agent`: string (optional) — ACP agent: opencode, claude, codex, copilot
  /// - `cwd`: string (optional) — Working directory for the ACP session
  /// - `auto_approve`: boolean (optional) — Automatically approve permission prompts
  /// - `model`: string (optional) — Model ID to use (e.g. claude-sonnet-4-5-20250514). Omit for agent default.
  /// - `context`: string (optional) — Comma-separated file paths or globs to include as context (e.g. 'agent.md,docs/*.md')
  ///
  /// Returns: Agent response text
  acp: func(
    prompt: string,
    agent: option<string>,
    cwd: option<string>,
    auto-approve: option<bool>,
    model: option<string>,
    context: option<string>
  ) -> result<string, string>;
}

world acp-world {
  export acp;
}
