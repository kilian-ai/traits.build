# @generated by gen-trait — DO NOT EDIT (re-run gen-trait to refresh)
#
# This trait wraps an external Component Model component imported via gen-trait.
# Source wasm:    c/hello-c.component.wasm
# WIT package:    hello:c
# WIT interface:  hello/hello

[trait]
description = "External component hello.c (imported via gen-trait)"
tags = ["external", "component"]

[signature]
params = [
  { name = "name", type = "string", description = "name" },
]

[signature.returns]
type = "string"
description = "Result returned by the foreign component (wrapped in result<T, string>)."

[implementation]
# `language` here is descriptive metadata — the actual implementation is the
# adjacent `c.component.wasm`, which the kernel's ComponentLoader picks
# up via filesystem path. Use whatever value reflects how the component was
# authored (rust, go, javascript, python, c, c++, …).
language = "rust"
source = "builtin"
entry = "c"
