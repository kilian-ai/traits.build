[trait]
description = "Shared pure-logic kernel library: type system, registry data model, TOML parsing, platform abstraction"
version = "v260326"
author = "system"
tags = ["kernel", "library", "types", "registry", "platform"]
provides = ["kernel/types", "kernel/registry-model", "kernel/platform"]

[signature]

[signature.returns]
type = "null"
description = "Library crate — not callable as a trait"

[implementation]
language = "rust"
source = "library"
entry = "lib"
callable = false
