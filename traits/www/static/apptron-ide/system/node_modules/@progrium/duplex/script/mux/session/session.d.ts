import * as codec from "../codec/mod.js";
import * as util from "../util.js";
import * as io from "../../io.js";
import * as session from "./mod.js";
export declare const minPacketLength = 9;
export declare const maxPacketLength: number;
export declare class Session {
    conn: io.ReadWriteCloser;
    channels: Array<session.Channel>;
    incoming: util.queue<session.Channel>;
    enc: codec.Encoder;
    dec: codec.Decoder;
    done: Promise<void>;
    closed: boolean;
    constructor(conn: io.ReadWriteCloser);
    open(): Promise<session.Channel>;
    accept(): Promise<session.Channel | null>;
    close(): Promise<void>;
    loop(): Promise<void>;
    handleOpen(msg: codec.OpenMessage): Promise<void>;
    newChannel(): session.Channel;
    getCh(id: number): session.Channel;
    addCh(ch: session.Channel): number;
    rmCh(id: number): void;
}
