"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CBORDecoder = exports.CBOREncoder = exports.CBORCodec = void 0;
const index_js_1 = require("../vnd/cbor-x-1.4.1/index.js");
class CBORCodec {
    constructor(debug = false, extensions) {
        Object.defineProperty(this, "debug", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        this.debug = debug;
        if (extensions) {
            extensions.forEach(index_js_1.addExtension);
        }
    }
    encoder(w) {
        return new CBOREncoder(w, this.debug);
    }
    decoder(r) {
        return new CBORDecoder(r, this.debug);
    }
}
exports.CBORCodec = CBORCodec;
class CBOREncoder {
    constructor(w, debug = false) {
        Object.defineProperty(this, "w", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        Object.defineProperty(this, "debug", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        this.w = w;
        this.debug = debug;
    }
    async encode(v) {
        if (this.debug) {
            console.log("<<", v);
        }
        let buf = (0, index_js_1.encode)(v);
        let nwritten = 0;
        while (nwritten < buf.length) {
            nwritten += await this.w.write(buf.subarray(nwritten));
        }
    }
}
exports.CBOREncoder = CBOREncoder;
class CBORDecoder {
    constructor(r, debug = false) {
        Object.defineProperty(this, "r", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        Object.defineProperty(this, "debug", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        this.r = r;
        this.debug = debug;
    }
    async decode(len) {
        const buf = new Uint8Array(len);
        let bufread = 0;
        while (bufread < len) {
            const n = await this.r.read(buf.subarray(bufread));
            if (n === null) {
                return Promise.resolve(null);
            }
            bufread += n;
        }
        let v = (0, index_js_1.decode)(buf);
        if (this.debug) {
            console.log(">>", v);
        }
        return Promise.resolve(v);
    }
}
exports.CBORDecoder = CBORDecoder;
