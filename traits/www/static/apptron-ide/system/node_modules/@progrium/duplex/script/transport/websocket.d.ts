import * as dntShim from "../_dnt.shims.js";
export declare function connect(addr: string, onclose?: () => void): Promise<Conn>;
export declare class Conn {
    ws: dntShim.WebSocket;
    waiters: Array<() => void>;
    chunks: Array<Uint8Array>;
    isClosed: boolean;
    constructor(ws: dntShim.WebSocket);
    read(p: Uint8Array): Promise<number | null>;
    write(p: Uint8Array): Promise<number>;
    close(): void;
}
