import * as io from "../io.js";
import * as codec from "./mod.js";
export declare class CBORCodec {
    debug: boolean;
    constructor(debug?: boolean, extensions?: any[]);
    encoder(w: io.Writer): codec.Encoder;
    decoder(r: io.Reader): codec.Decoder;
}
export declare class CBOREncoder {
    w: io.Writer;
    debug: boolean;
    constructor(w: io.Writer, debug?: boolean);
    encode(v: any): Promise<void>;
}
export declare class CBORDecoder {
    r: io.Reader;
    debug: boolean;
    constructor(r: io.Reader, debug?: boolean);
    decode(len: number): Promise<any>;
}
