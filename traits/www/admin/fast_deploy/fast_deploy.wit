// @generated from fast-deploy.trait.toml — DO NOT EDIT
// Regenerate with: cargo run -p gen-wit
//
// Trait path: www.admin.fast_deploy
// Source version: v260321.202512

package traits:www-fast-deploy@0.1.0;

/// Run fast-deploy.sh locally: build amd64 binary in Docker + upload via sftp + restart. Only works from local dev machine.
interface fast-deploy {
  /// Run fast-deploy.sh locally: build amd64 binary in Docker + upload via sftp + restart. Only works from local dev machine.
  ///
  /// Parameters:
  /// - `mode`: string (optional) — Mode: 'build' (full build+upload+restart), 'upload' (re-upload last binary+restart) [example: build]
  ///
  /// Returns: { ok, output, exit_code }
  fast-deploy: func(
    mode: option<string>
  ) -> result<string, string>;
}

world fast-deploy-world {
  export fast-deploy;
}
