[trait]
description = "Get or set persistent voice chat preferences (voice, model, agent) — changes apply immediately and become defaults"
version = "v260330.235250"
checksum = "60d019e2043acbf048fc5ca272f0e8ac624c0ccc62af9c57a756a2f2cb16c5c9"
author = "system"
tags = ["sys", "voice", "config", "preferences"]

[signature]
params = [
  { name = "action", type = "string", description = "get | set", required = true, example = "set" },
  { name = "key", type = "string", description = "Preference key: voice, model, or agent", required = true, example = "voice" },
  { name = "value", type = "string", description = "New value (required for set)", required = false, example = "shimmer" },
]

[signature.returns]
type = "map"
description = "{ok: bool, key, value, previous?}"

[implementation]
language = "rust"
source = "builtin"
entry = "voice_config"