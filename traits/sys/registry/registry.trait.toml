[trait]
description = "Registry read API — list, info, query, search, and inspect registered traits"
version = "v260320.224553"
epoch = 0
checksum = "569a5de7c9fa3b09b93b8ef55b77f870dda626bf60de1d70799bd92b92a4b988"
author = "system"
tags = ["system", "meta", "registry"]
provides = ["sys/registry"]

[requires]
globals = "kernel/globals"

[bindings]
globals = "kernel.globals"

[signature]
params = [
  { name = "action", type = "string", description = "Action: list|info|tree|namespaces|count|get|search|namespace", optional = true },
  { name = "arg", type = "string", description = "Argument for info/get/search/namespace/list actions", optional = true }
]

[signature.returns]
type = "any"
description = "Registry data (tree, namespace list, count, trait entry, or search results)"

[implementation]
language = "rust"
source = "builtin"
entry = "registry"