{
  "trait": "sys.ps",
  "description": "List running background tasks via the platform abstraction layer",
  "features": [
    {
      "name": "Returns process list on native",
      "description": "On native, sys.ps returns an object with ok and processes array",
      "tests": [
        {
          "name": "returns ok with processes key",
          "type": "command",
          "command": "./target/release/traits call sys.ps 2>/dev/null",
          "checks": [
            {
              "type": "exit_code",
              "expected": 0
            }
          ]
        }
      ],
      "examples": [
        {
          "input": [],
          "output": {
            "contains": ["\"ok\""]
          }
        }
      ]
    }
  ]
}
