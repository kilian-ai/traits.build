[package]
name = "trait-sys-ps"
version = "0.1.0"
edition = "2021"
description = "Process listing trait (cdylib plugin)"
publish = true

[lib]
path = "lib.rs"
crate-type = ["cdylib"]

[dependencies]
plugin_api = { path = "../../kernel/plugin_api" }
serde_json = "1"
libc = "0.2"
