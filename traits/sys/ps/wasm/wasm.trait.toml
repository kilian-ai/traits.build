[trait]
description = "WASM process status — reports runtime state in the browser where OS threads don't exist"
version = "v260325"
epoch = 0
author = "system"
tags = ["system", "process", "wasm", "browser"]
provides = ["sys/ps"]

[signature]
params = []

[signature.returns]
type = "object"
description = "{ ok, runtime: 'wasm', processes: [], wasm: { callable, registered, dispatch_modes } }"

[implementation]
language = "rust"
source = "builtin"
entry = "wasm_ps"
wasm = true
