[package]
name = "trait-sys-checksum"
version = "0.1.0"
edition = "2021"
description = "SHA-256 checksum trait (cdylib plugin)"
publish = true

[lib]
path = "lib.rs"
crate-type = ["cdylib"]

[dependencies]
plugin_api = { path = "../../kernel/plugin_api" }
serde_json = "1"
sha2 = "0.10"
regex = "1"
