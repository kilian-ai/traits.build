// Hand-mirrored from traits/sys/echo/echo.wit (gen-wit output) so this
// component crate is self-contained and not affected by gen-wit changes.

package traits:sys-echo@0.1.0;

interface echo {
  echo: func(
    text: string
  ) -> result<string, string>;
}

world echo-world {
  export echo;
}
