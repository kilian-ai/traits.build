// @generated from secrets.trait.toml — DO NOT EDIT
// Regenerate with: cargo run -p gen-wit
//
// Trait path: sys.secrets
// Source version: v260329

package traits:sys-secrets@0.1.0;

/// Encrypted secrets store — set, get, delete, list secrets with AES-256-GCM encryption at rest and zeroize-on-drop in memory
interface secrets {
  /// Encrypted secrets store — set, get, delete, list secrets with AES-256-GCM encryption at rest and zeroize-on-drop in memory
  ///
  /// Parameters:
  /// - `action`: string — Action: set | get | delete | list | resolve [example: list]
  /// - `id`: string (optional) — Secret identifier (e.g. 'stripe_key', 'db_password') [example: openai_key]
  /// - `value`: string (optional) — Secret value (only for 'set' action — never logged or returned) [example: sk-...]
  ///
  /// Returns: Result object — varies by action
  secrets: func(
    action: string,
    id: option<string>,
    value: option<string>
  ) -> result<string, string>;
}

world secrets-world {
  export secrets;
}
