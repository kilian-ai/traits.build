[trait]
description = "MCP stdio server — exposes all traits as MCP tools via JSON-RPC 2.0 over stdin/stdout"
version = "v260330"
checksum = "994f3c197588fef77a9e7b084707430227f2383ed03e8fdc94c697deb739ea1d"
epoch = 0
author = "system"
tags = ["system", "mcp", "ai", "protocol"]
provides = ["sys/mcp"]
background = false

[signature]
params = []

[signature.returns]
type = "null"
description = "Runs MCP stdio loop (blocks until stdin closes)"

[requires]
globals = "kernel/globals"
types = "kernel/types"
dispatcher = "kernel/dispatcher"

[bindings]
globals = "kernel.globals"
types = "kernel.types"
dispatcher = "kernel.dispatcher"

[implementation]
language = "rust"
source = "builtin"
entry = "mcp"