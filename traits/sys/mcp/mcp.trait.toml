[trait]
description = "MCP stdio server — exposes all traits as MCP tools via JSON-RPC 2.0 over stdin/stdout"
version = "v260322.173453"
checksum = "0497c09deb24740f03d8cebd45b412b7bdd7c3668201d7fb0087da093caf3e02"
epoch = 0
author = "system"
tags = ["system", "mcp", "ai", "protocol"]
provides = ["sys/mcp"]
background = false

[signature]
params = []

[signature.returns]
type = "null"
description = "Runs MCP stdio loop (blocks until stdin closes)"

[requires]
globals = "kernel/globals"
types = "kernel/types"
dispatcher = "kernel/dispatcher"

[bindings]
globals = "kernel.globals"
types = "kernel.types"
dispatcher = "kernel.dispatcher"

[implementation]
language = "rust"
source = "builtin"
entry = "mcp"