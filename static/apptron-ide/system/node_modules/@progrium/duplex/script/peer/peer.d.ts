import * as rpc from "../rpc/mod.js";
import * as codec from "../codec/mod.js";
import * as api from "../api.js";
export declare class Peer implements rpc.Caller, rpc.Handler {
    session: api.Session;
    caller: rpc.Caller;
    codec: codec.Codec;
    responder: rpc.RespondMux;
    constructor(session: api.Session, codec: codec.Codec);
    close(): Promise<void>;
    respond(): Promise<void>;
    call(selector: string, params: any): Promise<rpc.Response>;
    handle(selector: string, handler: rpc.Handler): void;
    respondRPC(r: rpc.Responder, c: rpc.Call): void;
    proxy(): any;
    virtualize(): any;
}
