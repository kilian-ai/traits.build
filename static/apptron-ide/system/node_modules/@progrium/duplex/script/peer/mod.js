"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __exportStar = (this && this.__exportStar) || function(m, exports) {
    for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports, p)) __createBinding(exports, m, p);
};
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.open = exports.connect = exports.options = void 0;
// @ts-ignore
__exportStar(require("./peer.js"), exports);
// @ts-ignore
const peer = __importStar(require("./peer.js"));
// @ts-ignore
const rpc = __importStar(require("../rpc/mod.js"));
// @ts-ignore
const mux = __importStar(require("../mux/mod.js"));
// @ts-ignore
const websocket = __importStar(require("../transport/websocket.js"));
// @ts-ignore
// import * as iframe from "../transport/iframe.ts";
exports.options = {
    transport: websocket,
};
async function connect(addr, codec) {
    const conn = await exports.options.transport.connect(addr);
    return open(conn, codec);
}
exports.connect = connect;
function open(conn, codec, handlers) {
    // if (conn === window.parent) {
    //   conn = new iframe.Conn();
    // }
    // if (typeof(conn) === "string") {
    //   conn = new iframe.Conn(document.querySelector(`iframe#${conn}`) as HTMLIFrameElement);
    // }
    const sess = new mux.Session(conn);
    const p = new peer.Peer(sess, codec);
    if (handlers) {
        for (const name in handlers) {
            p.handle(name, rpc.HandlerFunc(handlers[name]));
        }
        p.respond();
    }
    return p;
}
exports.open = open;
