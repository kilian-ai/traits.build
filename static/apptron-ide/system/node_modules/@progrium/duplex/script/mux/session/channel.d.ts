import * as util from "../util.js";
import * as codec from "../codec/mod.js";
import * as session from "./mod.js";
export declare const channelMaxPacket: number;
export declare const channelWindowSize: number;
export declare class Channel {
    localId: number;
    remoteId: number;
    maxIncomingPayload: number;
    maxRemotePayload: number;
    session: session.Session;
    ready: util.queue<boolean>;
    sentEOF: boolean;
    sentClose: boolean;
    remoteWin: number;
    myWindow: number;
    readBuf: util.ReadBuffer;
    writers: Array<() => void>;
    constructor(sess: session.Session);
    get id(): number;
    read(p: Uint8Array): Promise<number | null>;
    write(p: Uint8Array): Promise<number>;
    reserveWindow(win: number): number;
    addWindow(win: number): void;
    closeWrite(): Promise<void>;
    close(): Promise<void>;
    shutdown(): void;
    adjustWindow(n: number): Promise<void>;
    send(msg: codec.ChannelMessage): Promise<number>;
    handle(msg: codec.ChannelMessage): void;
    handleData(msg: codec.DataMessage): void;
}
