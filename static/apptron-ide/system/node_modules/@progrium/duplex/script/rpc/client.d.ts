import * as rpc from "./mod.js";
import * as codec from "../codec/mod.js";
import * as api from "../api.js";
export declare class Client implements rpc.Caller {
    session: api.Session;
    codec: codec.Codec;
    constructor(session: api.Session, codec: codec.Codec);
    call(selector: string, args: any): Promise<rpc.Response>;
}
export declare function VirtualCaller(caller: rpc.Caller): any;
