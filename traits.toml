[traits]
traits_dir = "./traits"
port = 8090
bind = "127.0.0.1"  # use "0.0.0.0" for Docker/production (set via TRAITS_BIND env)
timeout = 30

# [deploy]
# fly_app and fly_region are read from FLY_APP / FLY_REGION env vars.
# Set them here to override, or pass via env.
